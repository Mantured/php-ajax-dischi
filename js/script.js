const app = new Vue({
  el: "#app",
  data: {},
  created() {
    /* axios
            .get()
            .then((result) =>{
            this. = result.data.results;
            console.log(result.data.results);
            }).catch((error) => {
            console.error(error);
            });*/
  },
});
